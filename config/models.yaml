presets:
  gemini:
    label: Gemini API (정확·유료)
    provider: gemini
    text: gemini-2.5-pro
    vision: gemini-2.5-flash
    note: 지시 정확. GEMINI_API_KEY 필요. 토큰당 과금.
    concurrent_load: true
  claude:
    label: Claude API (정확·유료)
    provider: anthropic
    text: claude-opus-4-8
    vision: gemini-2.5-flash
    note: 지시 정확히 따름(마커 잘 나옴). ANTHROPIC_API_KEY 필요. 비전은 Gemini.
    concurrent_load: true
  claude_sonnet:
    label: Claude API — Sonnet (저렴)
    provider: anthropic
    text: claude-sonnet-4-6
    vision: gemini-2.5-flash
    note: Opus보다 저렴, 충분히 정확. ANTHROPIC_API_KEY 필요. 비전은 Gemini.
    concurrent_load: true
  gpt:
    label: OpenAI API — GPT (정확·유료)
    provider: openai
    text: gpt-4o
    vision: gemini-2.5-flash
    note: 지시 정확. OPENAI_API_KEY 필요. 비전은 Gemini.
    concurrent_load: true
default: gemini
selection:
  text: gemini-2.5-pro
  vision: gemini-2.5-flash
# 사진 '✨ AI 자동 추천'(온디맨드)에 쓰는 멀티모달 모델.
# Gemini Flash 권장 — 글 1개당 호출 1번(사진 전부 한 번에)이라 매우 저렴(글당 수 원).
# GEMINI_API_KEY 필요. 메모·메뉴·가게설명을 함께 넣어 '데미소스 돈까스'처럼 맥락으로 유추합니다.
caption:
  model: gemini-2.5-flash
